/**
 * Common class that contains data which is widely used throughout the game.
 * Only data which is used in multiple places should be stored here.
 * Additionally, stores settings such as keybindings.
 */

package dk.ee.zg.common.data;
